ImageDto:
  type: object
  description: |
    An image in a catalog, as metadata. The image itself is fetched through
    `GET .../images/{imageKey}/content`.

    A template places an image by referencing its `key` from an image node, as `props.assetId`
    alongside the owning catalog's `props.catalogKey`.
  required:
    - key
    - tenantId
    - catalog
    - catalogType
    - readOnly
    - name
    - mediaType
    - sizeBytes
    - createdAt
  properties:
    key:
      type: string
      pattern: '^[a-z0-9]+(-[a-z0-9]+)*$'
      maxLength: 50
      description: |
        The image's public key, unique within its catalog. It is the value a template references as
        `props.assetId`, the image's slug in a catalog export, and the `{imageKey}` that addresses
        the image in this API.

        An image this installation creates has a generated UUID as its key. An image from a catalog
        published elsewhere may have a readable one, such as `municipality-mark`. Treat it as an
        opaque string either way.
      example: "4a64f2f9-2c7a-4d26-9cf7-d34c7ce92280"
    tenantId:
      type: string
      pattern: '^[a-z][a-z0-9]*(-[a-z0-9]+)*$'
      example: acme-corp
    catalog:
      type: string
      pattern: '^[a-z][a-z0-9]*(-[a-z0-9]+)*$'
      description: Catalog that owns the image.
      example: default
    catalogType:
      type: string
      enum:
        - AUTHORED
        - SUBSCRIBED
      example: AUTHORED
    readOnly:
      type: boolean
      description: True when the image belongs to a SUBSCRIBED catalog.
      example: false
    name:
      type: string
      minLength: 1
      maxLength: 255
      example: logo.svg
    mediaType:
      type: string
      description: |
        The image's MIME type. Supported image types are deployment-configured; common ones are
        `image/png`, `image/jpeg`, `image/svg+xml`, and `image/webp`.
      example: image/svg+xml
    sizeBytes:
      type: integer
      format: int64
      minimum: 0
      maximum: 5242880
      example: 12482
    width:
      type:
        - integer
        - 'null'
      minimum: 1
      description: Width in pixels when known; null for SVG.
      example: 512
    height:
      type:
        - integer
        - 'null'
      minimum: 1
      description: Height in pixels when known; null for SVG.
      example: 128
    createdAt:
      type: string
      format: date-time
      example: "2026-07-23T10:00:00Z"

ImageListResponse:
  type: object
  required:
    - items
    - page
  properties:
    items:
      type: array
      items:
        $ref: '#/ImageDto'
    page:
      $ref: './common.yaml#/PageMeta'
