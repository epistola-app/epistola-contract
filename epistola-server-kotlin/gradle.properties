# API version from OpenAPI spec (major.minor)
# Update this when the OpenAPI spec version changes and reset patchVersion to 0
apiVersion=1.0

# Patch version, auto-incremented by CI on release
# Reset to 0 when apiVersion changes
patchVersion=0
